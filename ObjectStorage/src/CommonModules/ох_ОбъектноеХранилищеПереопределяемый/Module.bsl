#Область ПрограммныйИнтерфейс

// Позволяет конфигурации-потребителю переопределить имя общего модуля HTTP-коннектора.
//
// Параметры:
//  ИмяМодуляКоннектораHTTP - Строка - имя общего модуля HTTP-коннектора.
//
Процедура ПриОпределенииИмениМодуляКоннектораHTTP(ИмяМодуляКоннектораHTTP) Экспорт
	// Переопределяется в конфигурации-потребителе.
КонецПроцедуры

#КонецОбласти

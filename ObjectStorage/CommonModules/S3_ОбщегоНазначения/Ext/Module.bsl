#Область ПрограммныйИнтерфейс

Функция СтруктураВСтроку(Структура, РазделительКлючЗначение = "=", РазделительЭлементов = ";") Экспорт
	
	СтрокиПредставления = Новый Массив;
	
	Для Каждого Элемент Из Структура Цикл
		СтрокиПредставления.Добавить(Элемент.Ключ + РазделительКлючЗначение + Элемент.Значение); 
	КонецЦикла;
	
	Возврат СтрСоединить(СтрокиПредставления, РазделительЭлементов);
	
КонецФункции

#КонецОбласти